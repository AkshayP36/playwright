let multiply= (a:number,b:number)=>
{
    console.log(a*b);
}
multiply(4,3);

/*

function add(x:number,y:number):number
{
    return(x+y);
}

*/

let add= (x:number,y:number):number =>
{
        return(x+y);

}